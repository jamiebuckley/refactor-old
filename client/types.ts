import "reflect-metadata";

let TYPES = {
    Scene: Symbol("Scene"),
    Engine: Symbol("Engine"),
    ShadowGenerator: Symbol("ShadowGenerator"),
    Shadow: Symbol("Shadow"),
    GameScene: Symbol("GameScene")
}

export default TYPES;