/**
 * Responsible for initializing a scene,
 * placing initial objects and deciding on goals
 */
export class ScenePlanner {

    public plan(): void {
        //create the spawner and decide on a position
        //create the initial workers
        //create the material nodes on the map
    }
}